services:
  chroma:
    image: chromadb/chroma:latest
    volumes:
      - ./chroma_db:/data  
    ports:
      - "8080:8000"
  
  app:
    build:
      context: .
      dockerfile: docker-file
    volumes:
      - ./data:/app/data
      - ./chroma_db:/app/chroma_db
    ports:
      - "8501:8501"
   